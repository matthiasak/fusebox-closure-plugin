{
  "name": "fusebox-closure-plugin",
  "devDependencies": {
    "google-closure-compiler-js": "^20161201.0.0",
    "typescript": "^2.1.4"
  },
  "main": "./lib",
  "scripts": {
    "clean": "rm -rf .fusebox",
    "postinstall": "npm i fuse-box",
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "npm run build && git add . && git commit -am 'updates' && npm version patch && git push origin --all && npm publish"
  },
  "peerDependencies": {
    "fuse-box": "^1.3.43"
  },
  "keywords": [
    "closure compiler",
    "fuse-box"
  ],
  "license": "MIT",
  "dependencies": {
    "@types/node": "^6.0.57"
  },
  "version": "0.0.3"
}
