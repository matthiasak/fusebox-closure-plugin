{
  "name": "fusebox-closure-plugin",
  "devDependencies": {
    "typescript": "^2.4.1"
  },
  "main": "./lib",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "npm run build && git add . && git commit -am 'updates' && npm version patch && git push origin --all && npm publish"
  },
  "peerDependencies": {
    "fuse-box": "^1.3.51"
  },
  "keywords": [
    "closure compiler",
    "fuse-box"
  ],
  "license": "MIT",
  "dependencies": {
    "@types/node": "^7.0.8",
    "google-closure-compiler-js": "^20170218.0.0"
  },
  "version": "0.0.9"
}
